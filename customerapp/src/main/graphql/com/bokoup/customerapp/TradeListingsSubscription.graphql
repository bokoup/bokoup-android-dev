subscription TradeListingsSubscription {
    listingWithToken {
        id
        tokenSize
        seller
        price
        createdAtOnChain
        mintObject {
            ...MintInfoFragment
        }
    }
}
